# Backend environment variables for Render deployment

# Port Render will expose (optional; Render sets PORT automatically)
PORT=8000

# Set production mode to enable secure cookies
NODE_ENV=production

# MongoDB connection string (Atlas/cluster URI)
# Example: mongodb+srv://<user>:<pass>@<cluster>.mongodb.net
MONGODB_URL=

# Allow your Netlify frontend origin(s) for CORS
# Comma-separated list if multiple origins
# Example: https://cartify12.netlify.app
CORS_ORIGIN=

# JWT access token settings
ACCESS_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=15m

# JWT refresh token settings
REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_EXPIRY=7d

# Cloudinary (if you upload images)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
